What part of today's lesson made the most sense?
- How variables work. They allow information to be stored once and reused throughout the program. This makes the code easier to update, organize, and maintain.

What surprised you about input()?
- Nothing honestly. Nothing really surprised me. I have used input validation concepts in other programming languages before. Seeing Python use the input() function felt familiar, although the syntax is different.

How could a SOC analyst use a script like this in real life?
- A SOC analyst could use a script like this to quickly generate standardized incident reports during an investigation. As the tool becomes more advanced, the collected information could be validated and stored in a database to improve tracking, reporting, and historical analysis.

If you were improving this tool for a SOC, what would you add next?
- Right now, I do not have input validation created in the code. The program should verify that users enter valid information before creating the report. For example, severity should only accept predefined values such as Low, Medium, High, or Critical, and required fields should not be left blank. They should not be able to input any random value.